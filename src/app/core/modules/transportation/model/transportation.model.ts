interface Transportation {
  id: number;
  approval_number: string;
  approval_date: Date;
  from_id: number;
  to_id: number;
  done_date: Date;
  selah_id: number;
  dabet_id: number;
}

export default Transportation;
